{
  "name": "crud-guru-mern",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:frontend": "bun run --filter './apps/frontend' dev",
    "dev:backend": "bun run --filter './apps/backend' dev",
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter './apps/*' build",
    "preview": "bun run --filter './apps/*' preview",
    "test": "bun run --filter './apps/*' test"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
